# This file was autogenerated by uv via the following command:
#    uv export -o requirements.txt --no-hashes
# FastAPI
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Environment Variables
python-dotenv==1.1.1

# Validation
pydantic==2.11.7
pydantic-settings==2.10.1

# LangChain Core
langchain==0.3.27
langchain-core==0.3.74
langchain-community==0.3.27

# LangGraph
langgraph==0.6.5

# OpenAI
langchain-openai==0.3.30
openai==1.100.2

# Gemini
langchain-google-genai==2.1.9

# Ollama
langchain-ollama==0.3.6
ollama==0.5.3

# MongoDB
pymongo==4.14.1
motor==3.7.1

# Memory / Embeddings
chromadb==1.0.20

# Authentication
python-jose[cryptography]==3.5.0
# bcrypt directly rather than passlib: passlib 1.7.4 (its last release) probes
# the backend with a 73-byte secret, which bcrypt 5.x rejects outright, so
# CryptContext(schemes=["bcrypt"]) raises ValueError on first use.
bcrypt==5.0.0
# Required by OAuth2PasswordRequestForm and UploadFile -- FastAPI does not pull
# it in on its own, and without it /auth/token and /documents fail at runtime.
python-multipart==0.0.32

# HTTP Requests
httpx==0.28.1
requests==2.32.4

# Async Utilities
aiofiles==24.1.0

# Logging & Monitoring
loguru==0.7.3

# Utilities
orjson==3.11.2
tenacity==9.1.2

# Testing
pytest==8.4.1
pytest-asyncio==1.1.0

# Search Tools
tavily-python
duckduckgo-search

# RAG / PDF
pypdf